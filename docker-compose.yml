
services:
  app:
    build: ./src
    ports:
      - "8080:3000"
    depends_on:
      - db

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: hola_mundo
    ports:
      - "3306:3306"
